
可以,这个回答能拿一点分,但还不够。面试官可能会追问:为什么安全性高?你可以补充说:BCrypt 会自动加盐
,每次加密结果都不一样,还能抵抗彩虹
表和暴力破解。MD5太快了,而且没有盐,很容易被撞库。所以、
现在基本不用 MD5 存密码了。那我顺着再追问你一个细节:BCrypt 每
次 encode嗯你的思路接近了,但有个关键
点要纠正:BCrypt 不会把密文解析回原来的密码。正确过程是 
matches 会用 数据库里保存的哈希值里的盐,对用户输入的明文重新计
算哈希,然后对比结果,不是解密,而是重新算一遍再比。这是比较标准的回答。继续面试。问题
:既然BCrypt 每次 encode 结果都不一样,那数据库里为什么只存一个哈希值就够了?

我在 WebConfig 中通过 InterceptorRegistry 注册 LoginUserInterceptor，
让所有请求进入 Controller 前先经过拦截器校验。拦截器会从请求头获取 JWT，
解析用户 ID，并保存当前登录用户信息。